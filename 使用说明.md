# 雷达数据分析与三维可视化系统 使用说明

## 一、主界面介绍

软件窗口分为两大部分：左侧是**主显示区**，右侧是**控制面板**。

主显示区可以同时展示 1 个、2 个或 4 个雷达图像画面，每个画面独立显示不同的雷达产品。当前正在操作的画面带有红色边框。

右侧控制面板从上到下依次是：工具箱、视图窗口数量切换、VCP 模式和仰角层选择、雷达产品切换、文件列表及播放控制。

---

## 二、工具箱

工具箱位于右上方，包含 10 个常用工具按钮：

**指针** — 默认模式。点击雷达图像上的任意位置，底部状态栏会显示该点的经纬度、方位、距离和回波数值。也可以点击文件列表中的文件名来切换数据。

**手形** — 拖拽平移。点击后鼠标变成手形，按住左键拖动可以平移雷达图像，方便查看不同区域，通过滚动鼠标实现地图缩放。如果开启了同步功能，其他画面也会同步移动。

**复位** — 一键恢复。把图像恢复到初始位置和原始大小，清除所有平移和缩放。

**放大** — 点击后鼠标变成放大镜，再点击图像即可放大。最大可放大到 16 倍。

**缩小** — 点击后鼠标变成缩小镜，再点击图像即可缩小。最小缩回原始大小。

**画线** — 绘制剖面线。点击后鼠标变成十字，在图像上点击起点，拖动到终点松开，系统会自动弹出该两点连线的垂直剖面图（VCS），展示从地面到高空的雷达回波分布。

**测距** — 测量距离。点击后鼠标变成十字，在图像上点击起点，拖动到终点松开，即可看到两点之间的实际距离（公里）。

**网格** — 显示或隐藏极坐标参考网格。网格以雷达站为中心，每 50 公里一圈同心环，每 30 度一条辐射线。

**地图** — 显示或隐藏地理底图。底图可以是程序内置的行政边界地图，也可以是 GIS 格式的省界/国界矢量地图。

**停止/更新** — 实时监控模式开关。点击切换为"更新"模式时，程序会自动监测数据目录中的新文件并加载；切换为"停止"模式时，文件列表冻结，用于逐文件分析。

---

## 三、视图窗口数量

可以选择 1 个、2 个或 4 个画面同时显示：

- **1 画面**：全屏默认显示反射率产品，适合单一产品详细分析
- **2 画面**：左右并排，默认左边显示反射率，右边显示速度
- **4 画面**：2×2 排列，默认分别显示反射率、速度、谱宽和组合反射率

每个画面可以独立切换不同时次、不同雷达产品、不同仰角层数据，方便对比分析。

---

## 四、文件翻看

右侧面板底部是文件列表，显示当前数据目录中所有雷达数据文件。点击文件名即可加载该文件并在主画面中显示，当弹出对话框提示打开的雷达数据格式有错时，
需要到菜单“设置” → “站点信息” → “数据” → ”数据格式“中选择对应的数据格式。

**【上一个】和【下一个】**按钮用于逐个翻看文件，可通过键盘上下键来快速翻看。到达列表末尾时会自动循环到开头。

按住 Ctrl 键点击可以多选不连续的文件，按住 Shift 键点击可以连续选择一段文件，多选文件用于动画显示和导出动画功能。

---

## 五、循环动画播放

点击**【循环动画】**按钮，程序会自动按设定的时间间隔（可在站点信息中设置，默认 3 秒）逐一切换文件，形成连续的雷达回波动画效果。播放过程中按钮变为【停止】，点击即可停止播放。

如果开启了实时监控模式，播放到列表末尾时会自动等待并加载新到达的数据文件。

---

## 六、导出 GIF 动画

**操作步骤：**
1. 在文件列表中按住 Ctrl 或 Shift 键，选择至少 2 个文件
2. 点击菜单：文件 → 导出 → 导出动画
3. 在弹出的对话框中设置：
   - **时间间隔**：每帧之间的停顿时间（毫秒），越大动画越慢
   - **存放路径**：GIF 文件保存的位置
   - **多面板合成动画**：如果当前显示的是 2 画面或 4 画面，勾选后会将所有画面的内容合并到同一帧中（每个画面保留各自的色标）
4. 点击确定，等待进度条完成即可

生成的 GIF 文件保存在指定目录下，文件名为 `radar_anim_时间戳.gif`。

---

## 七、雷达产品切换

右侧面板中列出了所有可用的雷达产品，其中当雷达是双偏振雷达时才能切换双偏振参数，点击即可切换：

| 产品 | 含义 | 典型用途 |
|------|------|----------|
| REF（反射率）| 降水回波强度 | 判断降雨强度和范围 |
| VEL（速度）| 径向速度 | 判断风向风速 |
| SW（谱宽）| 速度谱宽 | 判断湍流和风切变 |
| VIL（垂直累积液水）| 整层液态水总量 | 冰雹预警 |
| TOP（云顶高度）| 回波顶高度 | 判断对流发展高度 |
| CAPPI（等高平面）| 固定高度切面 | 消除仰角差异的对比 |
| CR（组合反射率）| 各层最大值合成 | 综合回波强度 |
| DBT/ZDR/KDP/φDP/CC/SNR | 双偏振参数 | 更精细的粒子识别 |

VIL、TOP、CAPPI 为计算产品，不需要选择仰角层。CR 出现在仰角按钮中。双偏振产品需要双偏振雷达数据才能使用。

---

## 八、显示模式切换

菜单"显示"中提供三种渲染模式：

- **色斑图**：雷达回波以填充颜色块显示，是默认也最直观的方式
- **等值线**：以轮廓线显示，类似地形等高线
- **色斑叠加等值线**：两层同时显示，等值线叠加在色斑图上

点击"显示设置"可以弹出详细配置对话框，调整颜色范围、色标方案、地图模式（自定义/GIS/无底图）和背景颜色。

---

## 九、三维交互分析

### 9.1 启动三维分析

1. 先加载雷达数据文件（建议使用反射率或速度产品）
2. 点击菜单：分析 → 三维分析
3. 弹出提示"请在显示区域按住左键拖拽选择三维分析区域"
4. 在主画面中按住左键拖动，画出一个蓝色矩形选框
5. 松开鼠标，程序自动开始构建三维数据，随后弹出三维可视化窗口，并自动勾选体绘制显示并加载三维数据

**注意**：选框太小（小于 5 像素）会提示重新选择。

### 9.2 数据构建过程

程序会自动完成以下步骤：
1. 遍历所有仰角层，提取选框内的雷达回波数据点
2. 将每个数据点的位置转换为地理经纬度和海拔高度
3. 使用空间插值算法，在 60×60 的水平网格和约 10 层的垂直网格上重建三维回波场
4. 将结果送入三维显示窗口

### 9.3 三维窗口操作

- **旋转视角**：鼠标左键拖拽
- **缩放**：鼠标滚轮
- **平移**：鼠标右键拖拽
- **图层管理**：右侧面板可勾选/取消不同图层

### 9.4 可用图层

| 图层 | 说明 |
|------|------|
| 底图纹理 | 加载 `data/map/` 目录下的 JPG 地图或卫星影像作为底部参考 |
| 三维体渲染 | 雷达回波数据的立体显示（自动勾选），可调透明度 |
| 等值面 | 提取特定数值的曲面，可用于显示地形 |
| 二维切面 | 在三维空间中显示一个平面切面 |
| 三维模型 | 加载 OBJ 格式的三维模型文件 |

体渲染图层可以调整：不透明度曲线（传递函数）、亮度、渲染算法、数据值范围等参数。



## 十、剖面分析

### RHI — 距离高度显示

**操作**：菜单 → 分析 → RHI 距离高度显示（快捷键 Ctrl+H）

弹出窗口显示沿正北方向（方位角 0°）的垂直剖面：
- 横轴为距离（公里）
- 纵轴为高度（公里）
- 颜色代表回波强度

可以直观看到云团的垂直结构。窗口大小可自由调整，同时可打开多个 RHI 窗口。鼠标选择”指针“后在地图上滑动，
RHI自动读取鼠标所在位置的方位数据进行剖面显示，点击鼠标左键或者右键，暂停跟随鼠标读取数据显示剖面，
再次点击鼠标左键或者右键将继续跟随鼠标读取数据显示剖面。

### VCS — 任意两点剖面

**方式一**：点击工具栏的**画线**按钮，在图像上拖拽一条线，松开后自动弹出剖面图。

**方式二**：菜单 → 分析 → 任意两点剖面图（快捷键 Ctrl+L）。

VCS 窗口同样显示垂直剖面，且横轴为沿剖面线的距离。可以精确定义需要分析的方位和范围。

---

## 十一、PPI 图像导出（高质量出图）

**操作**：菜单 → 文件 → 导出 → PPI

弹出设置对话框，包含 6 个选项卡：

**分辨率**：设置输出图像的像素尺寸、DPI 和物理尺寸。例如 3000×3000 像素、300 DPI 可输出约 25cm 的印刷品质图片。背景可选白色、黑色或透明。

**坐标**：选择坐标类型（经纬度标注或方位距离标注），设置主刻度间隔和次刻度数量，调整坐标字体大小。

**地图**：选择 GIS 边界文件（如省界、国界、河流），设置线条颜色和粗细。点击".."按钮可浏览选择 shp 文件。

**色标**：选择色标放在图像右侧还是底部，设置色标字体大小和与地图的距离。

**标题**：开启或关闭标题，标题文字留空则自动生成（含雷达站名、时间、产品、仰角），可设置字体大小和与地图的距离。

**输出**：选择输出目录和格式（PNG 或 TIFF）。

设置完成后点击**预览**可查看效果，点击**确定**输出高清图像。所有设置会自动保存，下次打开时恢复。

输出的图像包含：白色背景、GIS 地图边界、经纬度坐标网格、雷达回波、色标、坐标轴刻度和标题，达到 SCI 论文插图质量标准。

---

## 十二、导出 GIF 动画

**操作步骤：**
1. 在文件列表中按住 Ctrl 或 Shift 多选至少 2 个文件
2. 菜单：文件 → 导出 → 导出动画
3. 设置帧间隔时间（毫秒）、输出路径
4. 如需将多画面内容合并到一帧，勾选"多面板合成动画"
5. 点击确定等待生成

---

## 十三、菜单功能速查

### 文件菜单
- **更新**：开启/关闭实时监控，自动加载新数据
- **另存为**：将当前画面保存为 JPG 图片
- **导出 → PPI**：高质量 PPI 图像导出，可直接用于论文发表
- **导出 → CAPPI/RHI/当前产品**：导出文本数据
- **导出 → 导出动画**：多帧 GIF 动画
- **导出 → 格式设置**：文本导出的坐标格式
- **页面设置/打印预览/打印**：打印当前画面
- **退出**：关闭程序

### 查看菜单
- **控制栏**：显示或隐藏右侧控制面板

### 同步菜单
- **工具箱操作同步**：将激活面板（外围红色线框）的平移缩放同步到其他面板
- **文件时间同步**：所有画面加载同一文件，切换文件时同步显示
- **仰角同步**：所有画面使用相同的仰角层，切换仰角同步显示

### 分析菜单
- **RHI 距离高度显示**：沿径向的垂直剖面
- **任意两点剖面图**：自定义两点连线的垂直剖面
- **反演风场**：利用变分反演方法从径向速度数据计算水平风场
- **三维分析**：三维体渲染雷达回波

### 显示菜单
- **色斑图/等值线/色斑叠加等值线**：画面渲染方式
- **显示设置**：颜色范围、色标方案、地图模式、背景颜色

### 叠加菜单
- **网格**：极坐标参考网格
- **地图**：地理边界底图
- **地形**：地形高程着色背景
- **风场**：风场叠加显示
- **区域**：自定义多边形区域

### 设置菜单
- **天擎配置**：数据下载服务参数
- **站点信息**：雷达站名称、经纬度、刷新间隔、数据保留天数
- **地理背景**：地图数据目录、图层开关、GIS 文件、地形文件
- **分辨率**：CR 和 VIL 产品的计算分辨率
- **显示设置**：同显示菜单中的显示设置

### 数据菜单
- **地面实况数据**：加载和显示地面观测数据
- **雷达基数据**：从服务器下载雷达数据
- **雷达拼图数据**：加载多站拼图产品
- **高空再分析数据**：加载 GRIB/NetCDF 再分析数据

### 帮助菜单
- **在线帮助**：打开项目主页
- **关于**：版本和作者信息

---

## 十四、快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+N | 实时监控开关 |
| Ctrl+A | 另存为 JPG |
| Ctrl+G | 网格显示/隐藏 |
| Ctrl+M | 地图显示/隐藏 |
| Ctrl+T | 地形显示/隐藏 |
| Ctrl+H | RHI 剖面 |
| Ctrl+L | 任意两点剖面 |
| Alt+F4 | 退出程序 |

---

## 十五、支持的数据格式

- **SC/SA/SB**：中国新一代天气雷达基数据（旧格式）
- **FMT**：标准格式雷达基数据
- **PARD**：标准格式双偏振雷达基数据
- **.bz2**：BZip2 压缩格式（自动解压），程序支持压缩格式和解压后的格式，为了显示速度高效，尽量先手动批量解压后再程序读取
- **NetCDF (.nc)**：地形高程数据、再分析数据
- **Shapefile (.shp)**：GIS 地理边界矢量数据
